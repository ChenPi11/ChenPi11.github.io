"use strict";export function setCookie(name,value,days){let expires="";if(days){let date=new Date;date.setTime(date.getTime()+days*24*60*60*1e3);expires="; expires="+date.toUTCString()}document.cookie=name+"="+(value||"")+expires+"; path=/"}export function getCookie(name){let nameEQ=name.trim()+"=";let ca=document.cookie.split(";");for(let i=0;i<ca.length;i++){let c=ca[i].trim();if(c.indexOf(nameEQ)==0){return c.substring(nameEQ.length,c.length)}}return null}export function listCookies(){let result=[];let cookies=document.cookie.split(";");for(let i=0;i<cookies.length;i++){let cookie=cookies[i];let eqPos=cookie.indexOf("=");let name=eqPos>-1?cookie.substring(0,eqPos):cookie;name=name.trim();let value=getCookie(name);result.push([name,value])}result.sort((function(a,b){if(a[0]<b[0]){return-1}if(a[0]>b[0]){return 1}}));return new Map(result)}
